---

# Short hostname
hostname: "{{inventory_hostname}}"

# FQDN
fqdn: "{{hostname}}.{{domain_name}}"

# Main IP address:  shortcut
ip_addr: "{{ansible_default_ipv4.address}}"

# Master host
master_host: "{{groups.coreos_master[0]}}"
master_fqdn: "{{groups.coreos_master[0]}}.{{domain_name}}"

# Manager user ID
manager_id: "manager"
