RKT_RUN_ARGS= \
  --uuid-file-save=/var/run/kubelet-pod.uuid \
  --volume var-log,kind=host,source=/var/log \
  --mount volume=var-log,target=/var/log \
  --volume dns,kind=host,source=/etc/resolv.conf \
  --mount volume=dns,target=/etc/resolv.conf" \
  --volume certs,kind=host,source={{k8s_ssl_dir}},readOnly=true \
  --mount volume=certs,target={{k8s_ssl_dir}} \
  --volume etcd-certs,kind=host,source={{etcd_cert_dir}},readOnly=true \
  --mount volume=etcd-certs,target={{etcd_cert_dir}} \
  --volume manifests,kind=host,source={{k8s_manifest_dir}},readOnly=true \
  --mount volume=manifests,target={{k8s_manifest_dir}} \
